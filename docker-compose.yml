version: '3.8'

services:
  pomodoro:
    build: .
    container_name: pomodoro-app
    ports:
      - "8000:8000"
    restart: unless-stopped
    # Environment variables can be passed here if needed later
    environment:
      - FLASK_DEBUG=false